[pytest]
pythonpath = .
# tests/unit  — pure unit tests, no DB/Redis required, runnable locally
# app/tests   — integration tests, require Docker (live Postgres + Redis)
testpaths = tests/unit app/tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session

